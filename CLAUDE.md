# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

VELA — a Spanish-language event marketplace (React + Vite SPA on the frontend, Supabase for auth/db/storage/edge functions). Attendees discover and buy tickets (`boletos`) for events (`eventos`); hosts (`anfitriones`) create events and get paid out via Mercado Pago. Deployed to Vercel (SPA rewrite in `vercel.json`).

## Commands

```
npm run dev       # start Vite dev server
npm run build     # production build (terser minify, console/debugger stripped, no sourcemaps)
npm run lint      # eslint . (flat config, eslint.config.js)
npm run preview   # preview a production build
```

There is no test runner configured in `package.json`. `loadtest/test.js` is a standalone k6 load-test script (`k6 run loadtest/test.js`) that hits the deployed Vercel URL, not local dev.

Supabase edge functions are deployed independently via the Supabase CLI (`supabase functions deploy <name>`), not through the Vite build.

## Architecture

**Single-file pages, no shared UI kit.** Everything lives in `src/pages/*.jsx` — each page is one large component file with inline `style={{...}}` objects (no CSS modules/styled-components/Tailwind classes in JS, even though Tailwind is a dependency). There's no component library. When editing a page, expect the whole page (layout, data fetching, forms, modals) in one file — `PanelAnfitrion.jsx` (~790 lines) and `Evento.jsx` (~555 lines) are the largest.

**Routing** is centralized in `src/App.jsx`, which also contains the entire logged-out/logged-in home page (`HomePage`) inline plus global auth state (`user`, `perfil` from the `profiles` table) fed via `supabase.auth.onAuthStateChange`. All routes are declared in one `<Routes>` block there — add new pages both as a file in `src/pages/` and a `<Route>` in `App.jsx`.

**Supabase is the backend.** `src/supabase.js` creates the client (publishable key is hardcoded there, not a secret — RLS enforces access control). Prefer `getUserSafe()` from that file over raw `supabase.auth.getUser()` for anything that gates page rendering: `getUser()` makes a network round-trip with no built-in timeout and can hang a page in a loading state indefinitely (especially right after a mobile app resumes from background); `getUserSafe()` races it against a short timeout and falls back to the cached local session.

**Data access pattern:** pages query Supabase tables directly with the JS client (`supabase.from(...).select(...)`), often joined (e.g. `eventos` joined with `profiles` and `boletos`). `@tanstack/react-query` is used for caching/loading state on read queries (see `HomePage`'s `eventos` query in `App.jsx`). Row-Level Security in Postgres is the real authorization boundary, not frontend checks — `es_admin` / `tipo` / `estado_anfitrion` flags on `profiles` gate UI visibility only.

**Privileged operations go through Supabase Edge Functions** (`supabase/functions/*/index.ts`, Deno), called from pages via plain `fetch` to `${VITE_SUPABASE_URL}/functions/v1/<name>` with the user's JWT as a Bearer token — not `supabase.functions.invoke`. Each function creates its own `service_role` Supabase client server-side. Use an edge function (not a direct table write) whenever an operation needs to: touch a secret (Mercado Pago host token, refunds), act across a permission boundary (host approving/rejecting another user's ticket request), or needs custom rate limiting — functions manually rate-limit via a `rate_limits` table (see `crear-pago-mp` and `gestionar-solicitud` for the pattern: count rows in the last hour window, insert one row per call, reject over a threshold).

Current functions: `crear-pago-mp` (creates an MP checkout preference, looks up the host's `mp_access_token`, applies a 10% marketplace fee), `confirmar-pago-mp` (verifies a completed payment directly against the MP API using the host's token, then activates the boleto — called from `PagoExitoso.jsx` instead of trusting the `collection_status` return URL param), `gestionar-solicitud` (host approves/rejects a requested ticket, triggers MP refund on reject), `hacer-checkin` (host marks a boleto as checked in; validates ownership, `estado = "activo"`, and blocks double check-in), `cancelar-evento`, `eliminar-cuenta`, `guardar-resena`, `reportar-evento`, `resolver-reporte`, `mp-oauth` (OAuth flow for hosts connecting their Mercado Pago account).

Note: `supabase/config.toml` still lists old function names (`crear-preferencia`, `webhook-pago`, `crear-pago-stripe`, `stripe-connect-onboarding`) that no longer exist under `supabase/functions/` — the project migrated from Stripe to Mercado Pago as the payment processor; Stripe packages remain in `package.json` but are unused in `src/`. Don't assume `config.toml`'s function list is authoritative — check the `functions/` directory instead.

**Payments:** Mercado Pago only (despite Stripe SDKs still installed). Two ticket flows exist per event (`tipo_boleto`): `instantaneo` (ticket activates immediately on payment) vs solicitud/request-based (host must approve via `gestionar-solicitud` before the ticket is `activo`). Boleto states include `activo`, `rechazado`, and pending/requested states — check `boletos.estado` before assuming a ticket is valid.

**Env vars** (`.env`, Vite-prefixed): `VITE_STRIPE_PUBLIC_KEY`, `VITE_MP_CLIENT_ID`, `VITE_SUPABASE_URL`. Edge functions use their own Deno env vars (`SUPABASE_URL`, `SERVICE_ROLE_KEY`, `MP_ACCESS_TOKEN`, `MP_CLIENT_ID`, `SITE_URL`) configured in the Supabase project, not `.env`.

**Language:** all UI copy, route paths, DB table/column names, and most code comments are in Spanish (e.g. `/mis-boletos`, `eventos`, `boletos`, `anfitrion`). Keep new code consistent with this — don't introduce English table/column names or route paths.

## Cómo trabajar conmigo

- Soy principiante en programación, aprendo haciendo. Explica en términos simples cuando algo sea complejo.
- Para cambios pequeños en un archivo: dame el texto EXACTO a buscar con Ctrl+F y el texto EXACTO con el que reemplazarlo.
- Si el cambio es grande o riesgoso: dame el archivo completo para reemplazar con Ctrl+A.
- Cuando se necesite hacer push: dame los 3 comandos juntos (git add . / git commit -m "mensaje" / git push).
- Nunca me mandes a revisar logs de Supabase, tablas del dashboard, ni Network del navegador como primer paso de diagnóstico.
- Todo el testing se hace en producción (eventapp-flax.vercel.app), salvo que diga lo contrario.
- Cuidado especial con corchetes al escribir código — ha habido errores por corchetes mal cerrados.
- k6 se corre con: & "C:\Program Files\k6\k6.exe" run loadtest/test.js
- La extensión de Deno ya está instalada en VS Code.
- Los archivos deno.json deben ser UTF-8 (no UTF-16 LE) para que el deploy funcione.
- Docker no está corriendo pero los deploys de Supabase funcionan igual con el warning.

## Cuentas de prueba

- Admin: panel.admin2026eventapp@gmail.com
- Host de prueba: zoriro.shop@gmail.com
- Mi cuenta (aldodrc2007@gmail.com) NO se puede usar como host.

## Estado actual / pendientes

- **Sistema de check-in: terminado (2026-07-02).** Búsqueda por nombre/código en el Panel de Anfitrión, marcado de entrada vía Edge Function `hacer-checkin` (valida dueño del evento, `estado = "activo"`, y bloquea doble check-in). Incluye escaneo real de QR con cámara (librería `jsqr`, ver `iniciarEscaneo`/detección por `useEffect` en `PanelAnfitrion.jsx` — el stream se conecta al `<video>` en un efecto, no en un `setTimeout`, porque en celulares más lentos el timeout podía ganarle al render y dejar la cámara en negro) y exportación a CSV de asistentes/check-in (`exportarCSV`, sin librerías extra).
- **Duración de evento configurable (2026-07-02).** Reemplazó las "5 horas" fijas que estaban hardcodeadas en 6 archivos distintos. Cada evento ahora tiene `duracion_horas` (obligatorio, máx. 24h, con qué tanto dura activo/visible) y `tiempo_registro_horas` (opcional, máx. 24h, solo informativo — no bloquea el check-in — si se deja vacío usa la misma duración). Lógica centralizada en `src/eventoUtils.js` (`eventoFinalizado`, `registroFinalizado`). Al pasar la duración: el evento desaparece de Explorar, su estatus en Panel de Anfitrión cambia a "Finalizado", y sus boletos se muestran como "Usado" en Mis Boletos. "Eventos destacados" en la home sigue con su comportamiento anterior (desaparece al arrancar el evento, no al finalizar) — decisión explícita del usuario, no un descuido.
- **Seguridad reforzada (2026-07-02):** token de Mercado Pago del anfitrión (`mp_access_token`) movido de `profiles` (legible por cualquier usuario vía RLS) a una tabla nueva `mp_credenciales` sin políticas RLS públicas (solo Edge Functions con `service_role`). Conexión OAuth de Mercado Pago protegida contra CSRF (el `state` ya no es el `user.id` directo — ahora es un código de un solo uso en `mp_oauth_state`). Rate limits agregados en `eliminar-cuenta`, `resolver-reporte`, `mp-oauth`. Activación de boletos de pago ya no confía en el parámetro `collection_status` de la URL de retorno de Mercado Pago — `PagoExitoso.jsx` llama a la Edge Function `confirmar-pago-mp`, que verifica el pago real contra la API de MP. Se eliminaron las políticas RLS de `UPDATE` en `boletos` (todas las escrituras pasan por Edge Functions con `service_role`). Precio > 0 sin Mercado Pago conectado ahora se bloquea también del lado del servidor (antes solo la UI lo prevenía). **Foto de registro (2026-07-02, corregida el 2026-07-05):** cuando el usuario se registra sin sesión activa (falta confirmar correo), la foto se subía originalmente vía `localStorage` — `App.jsx` la subía en cuanto hubiera una sesión real, pero eso solo funcionaba si el usuario confirmaba el correo en el MISMO dispositivo/navegador donde se registró. **Bug real encontrado en producción:** un usuario se registró en PC y confirmó el correo desde el celular — la foto nunca se subió en el celular (sin la entrada de `localStorage` ahí) y quedó huérfana en la PC hasta que volvió a abrir sesión ahí. Corregido con una Edge Function nueva, `guardar-avatar-registro` (pública, sin sesión — se llama justo después de `signUp`, antes de que exista cualquier JWT), que sube la foto con `service_role` de inmediato y actualiza `profiles.avatar_url` en ese momento, sin depender de qué dispositivo confirme el correo después. Seguridad: valida con `auth.admin.getUserById` que el `user_id` sea una cuenta real, recién creada (<1h) y sin confirmar todavía — evita que alguien mande un `user_id` ajeno para sobreescribir la foto de otra persona. `App.jsx` ya no tiene la lógica de `localStorage`/`subirAvatarPendiente`, quedó eliminada por completo.
- **RLS y Storage endurecidos (2026-07-02):** la política de `INSERT` en `eventos` no verificaba nada (`with_check: true`) — cualquier usuario autenticado podía crear un evento a nombre de otro anfitrión; ahora exige `auth.uid() = anfitrion_id` y que el perfil sea anfitrión aprobado. Se quitó la política de `DELETE` en `eventos` (un anfitrión podía borrar su evento directo, saltándose los reembolsos de `cancelar-evento`) — cancelar un evento ahora depende 100% de esa Edge Function. La política de `UPDATE` en `eventos` usaba 5 horas fijas en vez de `duracion_horas` — ya corregida. Se quitó la política de `UPDATE` en `resenas` (edición de reseñas ya solo pasa por `guardar-resena`, que sí limita caracteres y rate limit). El bucket de Storage `ine-docs` (identificaciones oficiales) era **público** — ahora es privado, con URLs firmadas temporales generadas en `Admin.jsx` al revisar solicitudes de anfitrión. Las políticas de `avatars`/`event-images`/`ine-docs` ahora exigen que el nombre del archivo empiece con el `user.id` de quien sube, para que nadie pueda subir/borrar el archivo de otra persona. Se agregó política de `DELETE` en `boletos` para que los boletos abandonados en pago (`pendiente_pago`) sí se puedan limpiar.
- **Cooperadores de check-in (2026-07-02):** un anfitrión puede generar un link de invitación (`/unirse-cooperador/:codigo`) para que otra persona ayude a hacer check-in sin necesitar cuenta en VELA — solo escribe su nombre. Tablas nuevas `invitaciones_cooperador` y `cooperadores_evento`, límite de 20 cooperadores por evento. Edge Functions nuevas `unirse-cooperador` y `buscar-checkin-cooperador`; `hacer-checkin` ahora acepta tanto la sesión del anfitrión como un `cooperador_id`. Como no hay sesión de usuario, estas llamadas mandan la publishable key de Supabase como `Authorization: Bearer` (ver `supabaseKey` exportado desde `src/supabase.js`) para pasar la verificación de la puerta de entrada de Supabase — la seguridad real la hace cada función revisando que el `cooperador_id`/`codigo` siga válido, no el JWT. El anfitrión puede revocar el link o quitar a un cooperador puntual en cualquier momento desde el botón "Cooperadores" del Panel de Anfitrión.
- **Resuelto (2026-07-03) — pago con tarjeta rechazado:** la causa era el monto de prueba, no la cuenta ni el código. Mercado Pago México exige un mínimo de **$5 MXN** en todos los medios de tarjeta (`min_allowed_amount: 5` en `/v1/payment_methods`); los eventos de prueba de $2 MXN caían debajo del mínimo y el checkout rechazaba la tarjeta en tiempo real con "La operación no acepta este medio de pago" (y "Dinero disponible" fallaba al procesar). La teoría anterior de "aprobación pendiente de MP para marketplace" quedó desmentida: `/v1/payments/search` muestra pagos con **tarjeta de débito** aprobados el 8–9 de junio ($6, $10 y $12 MXN, todos con `marketplace_fee` cobrado). Fix aplicado: `crear-pago-mp` rechaza montos < $5 con mensaje claro, la comisión ahora se calcula sobre el total (`precio × cantidad`, antes ignoraba la cantidad), y `CrearEvento`/`PanelAnfitrion` bloquean precios de $1–4. **Pendiente real:** probar de punta a punta pago + reembolso con un evento de precio ≥ $5 y una tarjeta/cuenta que no sea del mismo dueño que el host (el auto-pago sí lo bloquea MP).
- **Reembolsos corregidos (2026-07-03):** `cancelar-evento` y `gestionar-solicitud` reembolsaban con `MP_ACCESS_TOKEN` (token de la plataforma), que NO tiene autoridad sobre los pagos cobrados por el anfitrión — probado empíricamente: MP responde 404 en `/v1/payments/{id}/refunds` con ese token, y 200 con el token OAuth del anfitrión. Ahora ambas leen el token de `mp_credenciales`. Además: se verifica la respuesta de cada reembolso (antes se ignoraba — `cancelar-evento` borraba boletos y evento aunque el reembolso fallara, perdiendo los `mp_payment_id`); si algún reembolso falla, NO se borra nada y el anfitrión ve el error (reintentable: los pagos ya reembolsados se detectan con un GET previo y no se cobran doble). Se deduplican los `mp_payment_id` (una compra de N boletos comparte un solo pago; antes se intentaba reembolsar N veces). En `gestionar-solicitud`, si el pago cubre varios boletos y solo se rechaza uno, se reembolsa solo el monto unitario (`precio × 1.10`). **Ojo:** el reembolso sale del saldo disponible del anfitrión en MP; sin saldo, MP lo rechaza (ahora el error sí se ve). **`resolver-reporte` tenía los mismos 2 bugs** (token de plataforma + borraba boletos/evento y marcaba el reporte como resuelto aunque el reembolso fallara) — corregida igual el 2026-07-03: reembolsa con el token del anfitrión del evento reportado, deduplica pagos, y si algo falla el reporte queda pendiente y nada se borra (Admin.jsx ya mostraba el error del backend, sin cambios de frontend). La protección natural contra "anfitrión retiró el dinero y luego cancela": MP retiene el dinero ~14 días antes de liberarlo al saldo retirable, y los reembolsos salen del dinero retenido; pasado eso, la cancelación queda bloqueada con error claro hasta que el anfitrión reponga saldo.
- **Flujo de solicitud con pago (2026-07-03):** en eventos `tipo_boleto = "solicitud"` con precio, el boleto pagado ya NO se activa directo — `confirmar-pago-mp` lo deja en `pendiente` (con `mp_payment_id` y `codigo_grupo` ya asignados) y el anfitrión decide en la pestaña Solicitudes: aprobar lo pasa a `activo`, rechazar dispara el reembolso automático vía `gestionar-solicitud` (si el reembolso falla, el boleto NO se marca rechazado). `PagoExitoso.jsx` muestra "Pago recibido, el anfitrión debe aprobar tu boleto" (usa el campo `estado` que ahora regresa `confirmar-pago-mp`), y la tarjeta de solicitud en el panel marca "💳 Boleto pagado — si lo rechazas, se reembolsa automáticamente". **Probado de punta a punta el 2026-07-04 — con un bug real encontrado y corregido en el camino:** al probar el flujo, un pago con débito quedó `in_process` (el banco pidió reintento diferido a MP, `status_detail: "deferred_retry"` — normal, no es error nuestro); el usuario reintentó la compra sin que el primer intento se limpiara, y `Evento.jsx` insertaba un segundo `pendiente_pago` sin revisar si ya había uno del mismo usuario+evento. Cuando el segundo pago sí se aprobó, `confirmar-pago-mp` activó **ambos** boletos pendientes de pago de ese usuario+evento con el mismo `mp_payment_id` (activa por usuario+evento, no por intento de compra específico) — quedaron 2 boletos "pendiente" compartiendo un solo pago. Al aprobar uno y rechazar el otro, el rechazo reembolsó el pago compartido completo (el monto coincidía con el total pagado), dejando el boleto aprobado "activo" con un pago ya reembolsado. **Corregido:** `Evento.jsx` borra cualquier `pendiente_pago` sobrante del mismo usuario+evento justo antes de insertar los boletos de un nuevo intento de compra (mismo criterio ya usado por la política RLS de `DELETE` en `boletos` para pendientes abandonados) — con eso nunca puede haber más de un lote de pendientes por usuario+evento a la vez, así que la activación por lote de `confirmar-pago-mp` vuelve a ser correcta. **El reembolso en sí (`gestionar-solicitud`) se confirmó funcionando correctamente con dinero real** — el pago pasó de `approved` a `refunded` verificado contra la API de MP; el problema era el boleto duplicado antes del reembolso, no el reembolso. **Pendiente real, deliberadamente no resuelto todavía:** si un pago queda `in_process`/`deferred_retry` y el banco lo aprueba horas después, hoy nada vuelve a preguntarle a MP — el boleto de ese intento se queda huérfano en `pendiente_pago` (invisible, inofensivo, pero el comprador se queda sin boleto aunque el banco sí cobre). **Resuelto (2026-07-04):** nueva Edge Function `mp-webhook` (pública, `--no-verify-jwt`, la llama MP directo desde sus servidores, no un usuario). `crear-pago-mp` ahora manda `notification_url: ${SITE_URL}/functions/v1/mp-webhook?evento_id=...` en cada preferencia — MP la llama sola cada vez que un pago cambia de estado, sin que el comprador tenga que estar en la app. La notificación en sí NUNCA se confía a ciegas (solo trae un id de pago): `mp-webhook` usa el `evento_id` de su propia URL (que nosotros pusimos, no el llamante) para buscar al anfitrión correcto en `mp_credenciales`, vuelve a consultar el pago real contra la API de MP con SU token, y cruza que `pago.metadata.evento_id` coincida con el de la URL antes de activar cualquier boleto — mismo principio que `confirmar-pago-mp`. Solo actúa si `pago.status === "approved"`; si sigue `in_process` o el evento ya no existe, responde 200 sin tocar nada (para que MP no reintente algo que no es un error). El `UPDATE` de boletos es idéntico y por tanto tan idempotente como el de `confirmar-pago-mp` — si el comprador ya se confirmó solo por la vía normal, esta notificación no afecta ninguna fila. Probado en vivo contra pagos reales (uno `in_process`, uno con `evento_id` no correspondiente al pago — rechazado correctamente —, y uno de un evento ya borrado) sin tocar dinero ni datos. **Firma HMAC agregada (2026-07-06):** `mp-webhook` ahora valida el header `x-signature` (manifest `id:...;request-id:...;ts:...;` firmado con HMAC-SHA256 contra `MP_WEBHOOK_SECRET`) antes de procesar cualquier notificación — si no coincide, responde 401 sin gastar la llamada a la API de MP. La verificación real contra la API sigue siendo la garantía de fondo (nadie puede forzar una activación falsa sin un pago real aprobado); la firma es una capa extra de eficiencia para rechazar notificaciones falsas más barato. El secreto se generó y configuró desde el panel de Mercado Pago (Tus integraciones → Webhooks → "Configurar notificaciones", URL `${VITE_SUPABASE_URL}/functions/v1/mp-webhook`, evento "Pagos" únicamente) y se guardó como secret de Supabase (`supabase secrets set MP_WEBHOOK_SECRET=...`). Si el secreto no estuviera configurado, la verificación se omite sin bloquear nada (compatibilidad hacia atrás).
- **Optimización de carga (2026-07-04):** la home y Explorar bajaban TODAS las filas de `boletos` de cada evento solo para contar asistentes (costo creciendo con eventos × boletos, pagado por cada visitante) — ahora usan `boletos(count)` con filtro `boletos.estado=eq.activo` (la base cuenta, el navegador recibe un número; los consumidores leen `ev.boletos?.[0]?.count`). Explorar además prefiltra `fecha >= now()-24h` en el servidor (antes bajaba todos los eventos de la historia). La consulta de la home tiene `staleTime` de 1 min en react-query. Índices de soporte en `supabase/indices_carga.sql` (correr en el SQL Editor; Postgres no indexa FKs solo). Test de carga del backend real en `loadtest/backend.js` (el `test.js` viejo solo pegaba al CDN de Vercel): 150 usuarios concurrentes → 62 req/s, p95 133ms, 0% errores en el free tier. Cuello de botella real restante: las 5GB/mes de egress del free tier (revisar antes de un lanzamiento con tráfico real). **SMTP propio ya configurado** (el límite bajo de correos de confirmación del SMTP integrado de Supabase ya no aplica).
- **Deudas técnicas menores resueltas (2026-07-04):** el precio sin el 10% de comisión (mismo bug que "Eventos destacados") también aparecía en `Explorar.jsx`, `MisBoletos.jsx` y `Perfil.jsx` — las tres muestran precio a quien compra/ya compró, así que ahora usan `Math.round(precio * 1.10)` igual que el resto de la app. Warnings de ESLint (`react-hooks/exhaustive-deps` de `navigate`, estable entre renders vía `useNavigate()`) corregidos en `PanelAnfitrion.jsx`, `Perfil.jsx` y `SerAnfitrion.jsx` agregándolo al arreglo de dependencias. El error `react-hooks/set-state-in-effect` en la lógica de escaneo QR (`PanelAnfitrion.jsx` y `UnirseCooperador.jsx`) y los falsos positivos de `exhaustive-deps` sobre `buscarCheckin`/`buscar`/`detenerEscaneo` se silenciaron con comentarios `eslint-disable` explicados en el código — **a propósito no se restructuró esa lógica**: son funciones recreadas en cada render (no `useCallback`), así que incluirlas como dependencia reiniciaría el ciclo de captura de frames en cada render y podría reintroducir el bug de cámara en negro en celulares lentos que ya está documentado arriba. **Limpieza de lint de todo el proyecto (2026-07-04), a petición explícita del usuario tras el hallazgo de arriba — `npm run lint` queda en 0 errores/warnings:**
- **Bug real encontrado y corregido:** `CategoryCard` en `App.jsx` recibía la prop `activa` (si esa categoría es el filtro seleccionado) pero nunca la usaba — el resaltado visual de "categoría activa" solo reaccionaba a hover/touch, nunca a la selección real. Ahora `active = hovered || touched || activa`. Verificado en el navegador: al hacer clic en una categoría, la tarjeta sí cambia a su estilo activo (gradiente morado).
- **Bugs de categorías en celular corregidos (2026-07-05):** `CategoryCard` disparaba el `onClick` del padre desde DOS lugares a la vez (`onTouchEnd` y `onClick`, este último con un filtro `e.isTrusted` que no distingue un tap real de su clic sintético) — en celular, un solo toque a veces activaba y desactivaba la categoría en el mismo instante, así que había que tocar dos veces para que se notara el cambio. Corregido dejando un solo disparador (`onClick={onClick}` directo); `onTouchStart`/`onTouchEnd` ya solo controlan el estado visual de "presionado". Además, cambiar de categoría en la Home hacía que la sección de Eventos mostrara "Cargando eventos..." por un instante — la cuadrícula se encogía de golpe y el navegador empujaba el scroll hacia arriba porque ya no cabía (muy notorio en celular, donde una sola columna de tarjetas altas colapsaba a un mensaje corto). Corregido con `placeholderData: keepPreviousData` de react-query — la lista anterior se queda visible mientras llega la nueva, sin hueco ni salto. Verificado en el navegador en viewport móvil: un clic activa/desactiva correctamente sin necesitar un segundo toque, y la altura de la página no cambia al cambiar de categoría. **Probado en dispositivos reales:** en iPhone quedó resuelto por completo; en un Xiaomi Android, la selección de categoría y el filtrado de eventos ya funcionan bien a la primera, pero la etiqueta morada junto a "Explorar categorías" (`· NombreCategoría`) tardaba ~3 toques en actualizarse — visual únicamente, no afectaba la función real. Se quitó esa etiqueta (a petición del usuario): la misma información ya se repetía en el encabezado de la sección de Eventos (`Eventos · NombreCategoría`), que sí actualiza bien en todos los dispositivos probados.
- **Íconos de categoría corregidos (2026-07-05):** el ícono de "Deportes" en `categoryIcons` (`App.jsx`) era literalmente un globo terráqueo (el ícono "globe" de Feather Icons) — no tenía nada que ver con deportes, probablemente un copy-paste equivocado. Reemplazado por un trofeo. El de "Autos" era el ícono "truck" (camión de mudanza) — reemplazado por un ícono de carro (sedán). Ambos en el mismo estilo de línea (`strokeWidth 1.8`, viewBox `0 0 24 24`) que el resto de los íconos de categoría, verificado visualmente en el navegador.
- **`Evento.jsx`:** `BloqueCompra` (la tarjeta de compra) estaba definido como un componente dentro del render y se invocaba como `<BloqueCompra />` dos veces (layout móvil y escritorio) — React lo recreaba como un tipo de componente nuevo en cada render, forzando remount/pérdida de estado interno en cada re-render. Se cambió a una variable con el JSX ya calculado (`bloqueCompra`, sin paréntesis de invocación) reusada en ambos lugares — mismo árbol de elementos, sin la recreación. También se eliminó `precioMostrado`/`precioTimerRef`, un debounce de precio que quedó muerto (el precio mostrado real usa `precioTotal`, una variable derivada, no ese estado).
- **`Admin.jsx`:** `cargarSolicitudes`/`cargarAnfitriones`/`cargarReportes` estaban declaradas después del `useEffect` que las llama al montar — funcionaba en tiempo de ejecución (el efecto corre después de que el cuerpo del componente ya asignó esas constantes) pero es fràgil ante refactors futuros; se reordenaron antes del efecto, sin cambiar nada más.
- **`ParticleHero` (`index.jsx` y `useScrollAnimation.js`):** dos casos de "estado derivado de otro estado, sincronizado por un efecto" — `scrollDistance` (derivaba de `isMobile`) y `reducedMotion`/`isComplete` (derivaban de `matchMedia`) ahora se calculan directamente durante el render o con inicializador perezoso de `useState` (mismo patrón ya usado en `useIsMobileViewport` de ese mismo archivo) — mismo resultado, un render menos cuando cambian, sin efecto de por medio.
- **`Explorar.jsx`:** los filtros que llegan por la URL (`?q=...&estado=...&fecha=...`) se aplicaban vía `useEffect` + `setState`; se cambió al patrón oficial de React "ajustar estado durante el render" (comparar `location.search` contra el último aplicado) — evita el parpadeo de un frame sin filtros. Se quitó `asistentes` (variable calculada pero nunca mostrada — las tarjetas de Explorar no muestran conteo de asistentes, a diferencia de la Home) y, con eso, también `boletos(count)` de la consulta a Supabase (ya no hacía falta pedir esos datos).
- **Limpieza menor sin riesgo:** imports/variables sin usar en `App.jsx` (`lazy`, `Suspense`, `useQueryClient`, `Link`, `AnimatePresence`, `menuAbierto`/`setMenuAbierto` — estado muerto sin ningún botón de menú que lo usara —, `px`), y en `MisBoletos.jsx` (estado `user` que se guardaba pero nunca se leía — todas las consultas ya usaban la constante local del efecto). `navigate`/`searchParams` agregados como dependencias correctas en los `useEffect` de verificación de sesión de `Admin.jsx`, `CrearEvento.jsx`, `MisBoletos.jsx`, `PagoExitoso.jsx` y `PagoFallido.jsx` (son referencias estables o de bajo riesgo de re-disparo; sin cambio de comportamiento). Verificado con `npm run build` y en el navegador (Home, Explorar con y sin parámetros de URL, y la página de un evento en layout móvil y escritorio) sin errores de consola.
- **Hero rediseñado: holograma en video (2026-07-05).** El efecto de partículas con scroll-jacking (`ParticleHero`) fue reemplazado por `src/components/HeroHolograma.jsx`: un video en bucle de un rayo holográfico girando (generado por el usuario con IA a partir de su imagen de referencia, procesado con ffmpeg vía `ffmpeg-static` en el scratchpad — nunca instalado en el sistema). Detalles técnicos del video: se recortó `trim 1.5–8.5s` del original (que giraba 540° con aceleración/frenado, por lo que un corte simple no cerraba el bucle), cerrado con `xfade` de 1s; marca de agua de Gemini eliminada con `delogo` (interpolación, no parche — un `drawbox` negro dejaba borde visible); calidad alta (mp4 crf 17 ≈1.5MB preferido como primer `<source>`, webm vp9 crf 30 ≈600KB de respaldo, poster jpg q2). Assets en `public/hero-bolt.{mp4,webm}` y `hero-bolt-poster.jpg`. **Fusión con la página (iterada 3 veces hasta quedar, 2026-07-05):** la silueta del cono de luz está HORNEADA en los archivos mp4/webm/poster con ffmpeg (máscara analítica vía `geq` — cono que se ensancha hacia arriba + elipse en la base, borde difuso de ~70px — multiplicada sobre cada cuadro con `blend=multiply`): fuera del cono el pixel es negro absoluto, y con `mix-blend-mode: screen` el negro no aporta nada, así que solo se pega a la página el área con luz, con orillas suaves. Intentos fallidos que NO repetir: (1) máscara radial CSS sobre el rectángulo — se notaba el rectángulo; (2) `clip-path` poligonal con la silueta — orillas duras visibles (el brillo ambiental cortado en línea recta); (3) darle a la sección del hero fondo negro + resplandores propios — bandas horizontales contra el fondo normal (feedback: "se ve muy cutre"). **La sección del hero NO tiene fondo propio** — la única transición es el video difuminándose sobre el fondo de siempre del `body`. **Barra de búsqueda de escritorio rediseñada** al estilo cápsula (contenedor `rgba(19,17,26,0.92)` con padding, campo interior redondeado, separadores de 1px, íconos morados `#8b6bff`, botón Buscar con gradiente morado propio — ya no usa `btn-3d`), misma funcionalidad (input + selects de estado/fecha + navigate a /explorar). **Anillo de texto 3D:** la frase (`FRASE` en HeroHolograma.jsx, hoy "vive lo que pasa cerca de ti" — el usuario planea cambiarla) gira alrededor del rayo colocada letra por letra sobre la circunferencia (99 spans con `rotateY+translateZ`, inclinación 8°; en grupos se veía como polígono); keyframes `velaRingSpin` en `index.css`. **En celular y con `prefers-reduced-motion` el `<video>` NI SE MONTA** (cero descarga): se muestra el poster + la frase como `<h1>` con degradado. En escritorio hay un `<h1>` sr-only con la frase (el anillo no es legible como texto). Se eliminaron del hero el título "Descubre eventos cerca de ti", el subtítulo largo y las 3 insignias (`heroBadges`/`BadgeIcon` borrados; `FeatureBadge` sigue vivo en la sección CTA de anfitriones). **`src/components/ParticleHero/` quedó huérfano** (sin imports) — como los módulos huérfanos ya quedan fuera del build de Vite (no afectan el peso del sitio), no se borró de inmediato; borrado el 2026-07-06 junto con `AvatarModal.jsx` (también huérfano) por limpieza de repo, a petición del usuario — verificado `npm run build`/`lint` en cero tras borrar ambos. La barra de búsqueda funcional no se tocó. Verificado en navegador: escritorio (video mp4 girando + anillo, sin rectángulo visible) y móvil (poster estático, sin video en el DOM), consola limpia, `npm run lint`/`build` en cero.
- **Bug real de comisión corregido (2026-07-06):** revisando un recibo real de MP, se detectó que `crear-pago-mp` calculaba `marketplace_fee` (nuestra comisión del 10%) como el 10% del precio YA inflado que paga el comprador (`Math.round(evento.precio * 1.10)`, que es lo que mandaba `Evento.jsx`), en vez del 10% del precio original del anfitrión. Eso equivale a un **11% real** sobre el precio del anfitrión, no 10% — un error constante y proporcional (no de redondeo) que se mantiene igual en cualquier precio: en un boleto de $1,000, el anfitrión pagaría ~$110 de comisión en vez de los $100 prometidos. **Corregido:** `Evento.jsx` ahora manda `evento.precio` (el precio original, sin inflar) a `crear-pago-mp`; la función calcula ella misma `precioConComision = Math.round(precio * 1.10)` (mismo lugar donde se valida el mínimo de $5 MXN, que ahora se checa sobre este precio con comisión, no sobre el original) y `marketplace_fee = Math.round((precioConComision - precio) * cantidad)` — el extra exacto que paga el comprador, para que el anfitrión reciba su precio completo (menos el cargo propio e inevitable de Mercado Pago) y la plataforma se quede con el 10% real. Antes había dos cálculos independientes de "+10%" (uno en `Evento.jsx` para mostrar/cobrar, otro en `crear-pago-mp` para la comisión) que coincidían por casualidad en montos chicos por el redondeo — ahora hay un solo cálculo, la función es la única fuente de verdad. Aparte, se confirmó que el "Cargo de Mercado Pago" (la comisión propia de MP, no la nuestra) en un recibo de $6 fue de $4.88 — consistente con un costo fijo (~$4 MXN) + pequeño porcentaje + IVA aplicado sobre el total, **no un bug**: en montos de prueba tan chicos ese costo fijo se come casi todo, pero baja mucho como proporción en precios reales (ej. ~5% en un boleto de $500).
- **Duda resuelta (2026-07-06) — reembolso no se refleja en el estado de cuenta de la tarjeta:** con dos recibos de MP del 3-4 de julio (venta de $6, reembolsada, `Total de la operación: $0`, estado "Devuelto") confirmados en el dashboard de MP tanto del lado del comprador como del anfitrión, pero SIN que ni el cargo original ni la devolución aparecieran en el resumen de las tarjetas usadas. Conclusión: el sistema de reembolsos (`cancelar-evento`/`gestionar-solicitud`/`resolver-reporte`, ver arriba) funcionó correctamente — MP registra el cobro y la reversión en su propio libro contable de inmediato, eso es lo que muestra el recibo. Que no se refleje en el estado de cuenta de la tarjeta es un tema del ciclo de liquidación bancario, fuera del control de la app: los cargos con tarjeta suelen pasar primero por una pre-autorización, y si el reembolso llega dentro de esa ventana (mismo día/pocos días después, como en este caso), el banco puede cancelar cargo y reversión entre sí antes de que ambos lleguen a facturarse — por eso ninguno de los dos llegó a verse. No es un bug del código; para una prueba concluyente habría que usar montos reales y dejar pasar 1-2 semanas antes de revisar el estado de cuenta.
- Términos de Uso, Política de Privacidad y Políticas de Seguridad (2026-07-02) actualizadas para reflejar cooperadores de check-in, registro de check-in, y que las reseñas son públicas — es un borrador consistente con lo que hace el producto, no reemplaza revisión de un abogado antes de lanzar.
- (Agrega aquí cualquier otro pendiente que tengas en mente)